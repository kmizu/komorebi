# OpenAI (for AI supervisor + guidance generation)
# Without this, the app uses rule-based supervision only (still functional)
OPENAI_API_KEY=

# ElevenLabs (for voice playback)
# Without this, guidance is text-only
ELEVENLABS_API_KEY=
# Voice ID — default is a calm English voice
ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL
